Write a program
// program.java

// that creates an Instructor class
class Instructor
    // that contains string that represent
    String first name
    String last name
    Integer courses

// that creates a Person class
class Person
    // that contains string that represent
    String first name
    String last name
    Integer age

// You will need to create a Queue class
class Queue
    // that will store each person
    // that will store each instructor
    Container Person
    Container Instructor

    // and can sort the queue based on
    sortLastName();        // quick sort
    sortAge();             // quick sort
    sortNumberofCourses(); // quick sort

main
    // PEOPLE
    // Prompt the user of the program to
    print("add five people to the queue")

    i goes from 1 to 5
        person = input("person {i}:")
        Queue.add(person)

    // Your program should provide the contents of the queue
    print(Queue)

    // then sort the queue using the quick sort in two ways:
    Queue.sortLastName(descending) // quick sort
    Queue.sortAge(descending)      // quick sort

    // INSTRUCTORS
    // Prompt the user of the program to
    print("add five instructors to the queue")

    i goes from 1 to 5
        instructor = input("instructor {i}:")
        Queue.add(instructor)

    // Your program should provide the contents of the queue
    print(Queue)

    //and then sort the queue using quick sort in two ways:
    Queue.sortLastName(descending)        // quick sort
    Queue.sortNumberofCourses(descending) // quick sort